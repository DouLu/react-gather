import React, { Component } from 'react';
export default class DragDemo extends Component {
  render() {
    return (
      <div>
        react lib drag and drop
        react lib drag and drop
      </div>
    );
  }
}